name: Deploy to Vercel and Supabase

on:
  push:
    branches:
      - main
      - sandbox
      - dev
  pull_request:
    branches:
      - main
      - sandbox

jobs:
  # Wait for Vercel deployment to complete
  wait-for-vercel:
    runs-on: ubuntu-latest
    outputs:
      deployment-url: ${{ steps.vercel-deployment.outputs.url }}
    steps:
      - name: Wait for Vercel Deployment
        uses: patrickedqvist/wait-for-vercel-preview@v1.3.1
        id: vercel-deployment
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          max_timeout: 600 # 10 minutes
          check_interval: 10 # Check every 10 seconds

  # Push database migrations to Supabase after Vercel deployment
  deploy-supabase:
    runs-on: ubuntu-latest
    needs: wait-for-vercel
    # Only run on sandbox or main branches
    if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/sandbox')
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      # Use different credentials based on branch
      SUPABASE_DB_PASSWORD: ${{ github.ref == 'refs/heads/main' && secrets.PRODUCTION_DB_PASSWORD || secrets.STAGING_DB_PASSWORD }}
      SUPABASE_PROJECT_ID: ${{ github.ref == 'refs/heads/main' && secrets.PRODUCTION_PROJECT_ID || secrets.STAGING_PROJECT_ID }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Link Supabase project
        run: supabase link --project-ref $SUPABASE_PROJECT_ID

      - name: Push database migrations
        run: supabase db push

      - name: Deployment Summary
        run: |
          echo "✅ Branch: ${{ github.ref_name }}"
          echo "✅ Environment: ${{ github.ref == 'refs/heads/main' && 'Production' || 'Staging' }}"
          echo "✅ Vercel deployment: ${{ needs.wait-for-vercel.outputs.deployment-url }}"
          echo "✅ Supabase migrations pushed successfully to ${{ github.ref == 'refs/heads/main' && 'production' || 'staging' }} project"

  # Post a comment on the PR with the deployment URL (only for PRs)
  comment-on-pr:
    runs-on: ubuntu-latest
    needs: wait-for-vercel
    if: github.event_name == 'pull_request'
    steps:
      - name: Comment on PR with Vercel URL
        uses: actions/github-script@v6
        env:
          DEPLOY_URL: ${{ needs.wait-for-vercel.outputs.deployment-url }}
        with:
          script: |
            const pr = context.payload.pull_request && context.payload.pull_request.number
            const url = process.env.DEPLOY_URL || '(no url returned)'
            if (!pr) {
              core.info('No pull request number available in the event payload. Skipping comment.')
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: pr,
                body: `✅ Deployment finished. Vercel preview: ${url}`
              })
            }
